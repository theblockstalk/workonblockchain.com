export interface ButtonGroupObject {
  label: string;
  exp_year: string;
}

export interface Options {
  name: string;
  value: string;
  checked: boolean;
}

export interface wizardObject {
  name: string;
  routerLink: string;
  disabled: boolean;
  active: boolean;
}
